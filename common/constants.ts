export enum EventType {
    SPORTS = 1000,
    VOLEYBALL = 1001,
    FOOTBALL = 1002,
    CRICKET = 1100,
    CULTURAL = 2000,
}

export enum Role {
    ADMIN = 'admin',
    USER = 'user',
    GUEST = 'guest',
}
